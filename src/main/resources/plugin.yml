name: Mounts
version: ${project.version}
main: com.acrylic.mounts.Mounts
prefix: Mounts
authors: [Acrylic]
description: Mounts.
commands:
  mount:
    aliases: [m, mounts]
    description: Main command for mounts.
    usage: /<command>
